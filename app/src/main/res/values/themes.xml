<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.KeyMod" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.KeyMod" parent="Base.Theme.KeyMod" />

    <style name="ButtonTheme" parent="Theme.AppCompat.Light">
        <item name="android:background">@drawable/key_background</item>
        <item name="android:textAllCaps">false</item>
    </style>

    <!-- Presentation Mode Button Theme - Dark Orange -->
    <style name="Theme.KeyMod.Presentation" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/presentation_primary</item>
        <item name="colorOnPrimary">@android:color/white</item>
    </style>
</resources>